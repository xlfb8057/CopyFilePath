# 问题交接文档 —— 右键复制绝对路径工具（Copy File Path）

> 本文档供接手的 AI / 开发者排查并解决该工具的安装与使用问题。
> 写于 2026-08-23。当前状态：**安装脚本能跑完并显示成功，但用户反馈右键菜单"用不了"**。
> 仓库根目录：`E:\AI-WorkSpace\Tools\Z1-00-鼠标右键复制绝对路径\`

---

## 1. 项目目标

做一个 Windows 右键菜单工具：在资源管理器里右键任意**文件 / 文件夹 / 文件夹空白处**，菜单出现 **「Copy File Path」**，点击即把该对象的**绝对路径**复制到剪贴板。

约束：
- 发布形态为**零编译**（用户下载后直接双击 `install.bat` 即用）。
- 写入 `HKEY_CURRENT_USER`（用户级），**不需要管理员权限**。
- 支持中文路径（用户是中文 Windows，目录本身含中文）。

---

## 2. 原始可用状态（重要背景）

最初的发布版 **v1.0（commit `142ec72`）是能用的**，只有一个缺点：复制后需要等几秒才得到路径（因为当时用 VBS + `clip.exe` 子进程 + 轮询，链路慢）。

也就是说：**核心功能（右键复制路径）本身在 v1.0 是验证过可用的**，只是"慢"。后续所有修改是为了"加速 + 便于发布 + 修各种报错"，但在这个过程中**把安装链路反复改坏，最终用户连基本功能都用不上了**。

接手时务必记住：不要推翻已实现的功能逻辑（`CopyPath.exe` 本身没问题），重点在**安装脚本 `install.bat` 是否把正确的注册表项写进去了**。

---

## 3. 完整修改时间线

| # | Commit | 做了什么 | 结果 |
|---|--------|----------|------|
| 1 | `142ec72` | 初始化 v1.0（VBS 慢链路，但能用） | 能用，但慢 |
| 2 | `00bdb71` | 新增 Setup.exe 安装器方案 | 被否，用户要最简单方式 |
| 3 | `1adb74a` | 移除 Setup，恢复 install.bat 零编译形态 | — |
| 4 | `41c4ba3` | install.bat 改用 PowerShell 生成临时 reg | PowerShell 退出太快，bat 一闪没 |
| 5 | `c7619e4` | 抽出 install.ps1，bat 调 ps1 | 仍有一闪问题 |
| 6 | `a63cfa6` | bat/ps1 改 UTF-8 BOM + chcp 65001 | 中文路径乱码，命令行被拆碎 |
| 7 | `46ee1d4` | 统一 CRLF + .gitattributes | — |
| 8 | `b2a941b` | bat 改回 GBK 无 BOM（修 @echo off 被 BOM 顶掉） | 安装成功提示恢复 |
| 9 | `f3acdbf` | **极简化**：去掉 PowerShell，纯 bat + `reg import` 写 HKCU | 安装脚本能跑完显示成功 |
| 10 | `4e3f687` | **修 install.bat 注册表 command 缺空格**（%EXE% 与 %1 粘连） | 当前最新，未验证 |

> 第 10 条是关键 bug：第 9 版的 `install.bat` 生成的注册表 `command` 值是 `"%EXE%"%1"`（缺空格），导致 exe 路径和参数粘连，Windows 无法正确调用。已在 `4e3f687` 补回空格，但**尚未在本机验证**。

---

## 4. 当前仓库文件清单

```
CopyPath.exe      3584 bytes   右键真正调用的运行程序（.NET Framework winexe，预编译）
CopyPath.cs         220 bytes   对应源码（可选，重编译用）
install.bat        2127 bytes   一键安装（纯 bat + reg import，免管理员）
uninstall.bat       781 bytes   一键卸载（纯 reg delete，免管理员）
README.md          5752 bytes   产品说明书
LICENSE            1078 bytes   MIT
.gitattributes      283 bytes   规定 *.bat 等 CRLF 换行
.gitignore          155 bytes   排除 .bak / Thumbs.db 等
问题交接文档.md             本文件
```

> 已删除：`install.ps1`、`install.reg`、`uninstall.reg`、`copy_path.vbs`、各种死代码。

---

## 5. 当前关键代码

### 5.1 `install.bat`（当前最新，已修空格）

```bat
@echo off
setlocal enabledelayedexpansion
:: ============================================================
:: Copy File Path - one-click install (no admin needed)
:: 1) copy CopyPath.exe to %APPDATA%\CopyFilePath\
:: 2) write HKCU context menu (file / folder / folder blank)
:: ============================================================

set "APP_DIR=%APPDATA%\CopyFilePath"
set "EXE=%APP_DIR%\CopyPath.exe"
set "SRC=%~dp0CopyPath.exe"

if not exist "%APP_DIR%" mkdir "%APP_DIR%"
copy /Y "%SRC%" "%EXE%" >nul
if not exist "%EXE%" (
    echo [ERROR] CopyPath.exe not found. Keep it next to install.bat.
    pause
    exit /b 1
)

:: build temp reg (replace %APPDATA% with real path) then import
set "TMPREG=%TEMP%\CopyFilePath_install.reg"
(
echo Windows Registry Editor Version 5.00
echo+
echo [HKEY_CURRENT_USER\Software\Classes\*\shell\CopyFilePath]
echo @="Copy File Path"
echo "Icon"="imageres.dll,-5302"
echo+
echo [HKEY_CURRENT_USER\Software\Classes\*\shell\CopyFilePath\command]
echo @="\"%EXE%\" \"%1\""
echo+
echo [HKEY_CURRENT_USER\Software\Classes\Directory\Background\shell\CopyFilePath]
echo @="Copy File Path"
echo "Icon"="imageres.dll,-5302"
echo+
echo [HKEY_CURRENT_USER\Software\Classes\Directory\Background\shell\CopyFilePath\command]
echo @="\"%EXE%\" \"%V\""
echo+
echo [HKEY_CURRENT_USER\Software\Classes\Directory\shell\CopyFilePath]
echo @="Copy File Path"
echo "Icon"="imageres.dll,-5302"
echo+
echo [HKEY_CURRENT_USER\Software\Classes\Directory\shell\CopyFilePath\command]
echo @="\"%EXE%\" \"%1\""
) > "%TMPREG%"

reg import "%TMPREG%" >nul 2>&1
if errorlevel 1 (
    echo [ERROR] registry write failed. Run as admin once, or retry under a Chinese path.
    pause
    exit /b 1
)
del "%TMPREG%" >nul 2>&1

echo.
echo ============================================================
echo [Copy File Path] Installed successfully!
echo   Right-click any file / folder / folder blank,
echo   choose "Copy File Path" to copy the absolute path.
echo   Run file: %EXE%
echo ============================================================
echo.
pause
```

> 第 31 / 38 / 45 行是 `command` 值，**必须**是 `"%EXE%" "%1"`（exe 路径和参数之间有空格）。这是第 10 次修复的核心。

### 5.2 `CopyPath.cs`（运行程序源码，未改动）

```csharp
using System;
using System.Threading;
using System.Windows.Forms;

class CopyPath {
    [STAThread]
    static void Main(string[] args) {
        if (args.Length == 0) return;
        Clipboard.SetText(args[0]);
    }
}
```

### 5.3 `uninstall.bat`

```bat
@echo off
setlocal
:: ============================================================
:: Copy File Path - uninstall (no admin; cleans HKCU install)
:: If you used an old HKLM version, right-click -> Run as admin.
:: ============================================================

reg delete "HKCU\Software\Classes\*\shell\CopyFilePath" /f >nul 2>&1
reg delete "HKCU\Software\Classes\Directory\Background\shell\CopyFilePath" /f >nul 2>&1
reg delete "HKCU\Software\Classes\Directory\shell\CopyFilePath" /f >nul 2>&1

set "APP_DIR=%APPDATA%\CopyFilePath"
if exist "%APP_DIR%" rd /s /q "%APP_DIR%" >nul 2>&1

echo.
echo [Copy File Path] Uninstalled. Menu entries and run file removed.
echo If Explorer still shows the old menu, sign out or reboot to refresh.
echo.
pause
```

---

## 6. 当前已知问题 / 卡住点（诚实说明）

用户最后反馈"还是用不了"，但**提供的截图是 `.cs` / `.gitignore` 文件双击无关联应用的弹窗，与工具功能无关**。因此我们无法从截图确认右键菜单的真实状态。可能的原因有三种，按概率排序：

1. **用户测试的是修复前的坏版本**：第 9 版（缺空格）曾被成功安装，注册表里是坏 `command`。用户后来可能没先 `uninstall` 再 `install`，导致坏项残留。
2. **Windows 11 右键菜单折叠**：Win11 默认右键先弹"新菜单"，旧式 `HKCU\Classes\*` 项藏在 **「显示更多选项」(Shift+F10)** 里，用户没展开就以为"没有"。
3. **真正的安装失败**：注册表没写进去，或 `CopyPath.exe` 因缺少 .NET Framework 而无法运行。

**我们不确定到底是哪一个**，需要接手者按第 9 节步骤逐一验证。

---

## 7. 环境假设（需向用户确认）

- 操作系统：中文 Windows，疑似 **Windows 11**（Win11 右键菜单折叠是高频坑）。
- 用户名：`A`（当前路径 `C:\Users\A\...`，英文，所以 `%APPDATA%` 展开不含中文，编码不是问题）。
- PowerShell 不可用：本机的安全策略禁止从某些上下文调用 `powershell.exe` / `cmd.exe`，所以**已彻底放弃 PowerShell 方案**，只用 `reg` 命令。
- Git 远程：`https://github.com/xlfb8057/CopyFilePath`，本地 `main` 领先若干提交（截至本文档，最新本地 commit `4e3f687` 尚未 `git push` 到 GitHub）。

---

## 8. 待接手 AI 验证 / 解决的具体清单

**P0（必须先做）**
1. 让用户先双击 `uninstall.bat`，再双击 `install.bat`（清掉可能的坏注册表项，装最新修复版）。
2. 验证注册表是否真的写入：
   - `Win+R` → `regedit` → 确认 `HKEY_CURRENT_USER\Software\Classes\*\shell\CopyFilePath` 存在，且其 `command` 子键的 `(默认)` 值**确实是** `"C:\Users\A\AppData\Roaming\CopyFilePath\CopyPath.exe" "%1"`（注意 exe 和 `%1` 之间的空格，以及两端的引号）。
3. 确认 `C:\Users\A\AppData\Roaming\CopyFilePath\CopyPath.exe` 文件确实存在。

**P1（右键测试）**
4. 右键一个普通文件 → 看菜单；如果是 Win11，先看主菜单有没有，没有就点 **「显示更多选项」** 再找 `Copy File Path`。
5. 点击 `Copy File Path` → 粘贴到记事本 → 路径是否正确复制。

**P2（健壮性，可选）**
6. 验证 `CopyPath.exe` 在用户机器能否独立运行：在 cmd 里手动跑 `CopyPath.exe "C:\test.txt"`，再粘贴看是否得到 `C:\test.txt`。若 `.NET Framework` 缺失会直接报错，需提示用户安装。
7. 考虑 `Clipboard.SetText` 在剪贴板被占用时可能抛异常（v1.0 没遇到，但值得加 try/catch 和重试，避免静默失败）。

---

## 9. 复现 / 验证步骤（给接手者）

```text
1. 双击 uninstall.bat          （清理旧项）
2. 双击 install.bat            （装最新版，应显示 Installed successfully!）
3. 按任意键关闭黑窗口
4. Win+R → regedit → 检查上面 P0-2 的注册表键值
5. 资源管理器右键一个文件（Win11 点"显示更多选项"）→ 找 Copy File Path → 点击
6. Ctrl+V 粘贴到记事本，确认路径正确
```

若第 4 步注册表键值不对（比如还是缺空格、或根本没写进去），问题就在 `install.bat` 的 `reg import` 环节，需进一步调试（可在 bat 里临时把 `reg import` 的 `>nul 2>&1` 去掉，看真实报错）。

---

## 10. 给接手者的建议

- **不要重写 `CopyPath.exe`**：核心程序没问题，v1.0 验证过可用。
- **安装脚本现在只剩 bat + reg**，逻辑已经很简单，重点验证 `reg import` 是否真的把正确的 `command` 写进去了。
- 如果 `reg import` 在用户机器上静默失败（bat 显示成功但实际没写），建议把 `install.bat` 第 48 行的 `reg import "%TMPREG%" >nul 2>&1` 改为 `reg import "%TMPREG%"`，让用户看到真实报错，再针对性解决。
- 关于"最简单方式"：用户明确要无脑双击即用。当前方案（bat + reg）已经是最简，不要再引入 Setup / PowerShell / 编译步骤。
```
